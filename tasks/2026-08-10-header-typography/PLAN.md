# PLAN: Header typography

Дата: 2026-08-10

## Шаги

1. `Header.tsx` — brand link `text-xl sm:text-2xl`.
2. `Header.tsx` — nav `text-base`.
3. `Header.tsx` — container `py-3 sm:py-3.5`.
4. Visual smoke: glass + solid + no-bg.
5. `REPORT.md`.

## Проверка

- `/en` с hero в CMS — glass header.
- Scroll — solid header.
- Без hero — light header.
